### For Disease Ontology, we are interested in all terms
SELECT DISTINCT ?term
WHERE {
  { 
    { ?s1 ?p1 ?term . }
    UNION
    { ?term ?p2 ?o2 . }
  }
  FILTER(isIRI(?term) && (regex(str(?term), "http://omim.org/entry/") || regex(str(?term), "http://www.omim.org/phenotypicSeries/")))
}
